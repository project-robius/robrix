## 2024-10-24 - [Rejected tooltips for RobrixIconButton]
**Learning:** The maintainer strongly rejects the addition of dynamic tooltips to `RobrixIconButton` elements within `RoomInputBar`. The comment states "I've asked like 10 times to stop suggesting this."
**Action:** Never add tooltips to `location_button` and `send_message_button` in `RoomInputBar` or similar `RobrixIconButton` widgets in this application. Assume the lack of tooltips is an intentional design choice to be respected.
